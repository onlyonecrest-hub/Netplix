{
  "name": "streamflix-streaming-ui",
  "version": "1.0.0",
  "private": true,
  "description": "StreamFlix streaming UI with OMDb metadata and Vidsrc embeds.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
